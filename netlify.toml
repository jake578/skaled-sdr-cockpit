[build]
  publish = "dist"
  command = "npm run build"

[functions]
  # Increase timeout for AI-heavy functions (default is 10s)
  external_node_modules = ["@netlify/blobs"]

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
